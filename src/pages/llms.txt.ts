import type {APIRoute} from 'astro';import {SITE} from '../site';export const GET:APIRoute=()=>new Response(`# CalcWise\n\nCalcWise is a free, multilingual collection of 400 browser-based calculators. Each tool page exposes its formula, method, category and related tools.\n\n- Site: ${SITE.domain}\n- Calculator directory: ${SITE.domain}/en/tools/\n- Editorial policy: ${SITE.domain}/en/editorial-policy/\n- Methodology: ${SITE.domain}/en/methodology/\n`,{headers:{'Content-Type':'text/plain'}});
